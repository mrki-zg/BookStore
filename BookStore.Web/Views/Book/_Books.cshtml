@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager

@model IList<Book>

<div class="card-deck">
    @foreach (var book in Model)
    {
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@book.Title</h5>
                <h6 class="card-subtitle mb-2 text-muted">@book.Author.FirstName, @book.Author.LastName</h6>
                <p class="card-text">@book.Summary</p>
            </div>

            <div class="card-footer text-right">
                <span class="font-weight-bold align-middle">Price: </span>
                <span class="text-muted align-middle">@book.Price €</span>

                @if (SignInManager.IsSignedIn(User))
                {
                    <button class="btn btn-primary ml-1">Buy</button>
                }
            </div>
        </div>
    }
</div>